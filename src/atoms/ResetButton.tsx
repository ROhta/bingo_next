import type { FC } from "react"

const ResetButton: FC = () => {
    return <button type={`button`} id={`reset-button`} className={`bg-slate-100/50 px-8 py-6 rounded text-3xl`}>RESET</button>
}

export default ResetButton